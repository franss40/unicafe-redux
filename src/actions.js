const good = () => {
  return { type: "GOOD" }
}

const ok = () => {
  return { type: "OK" }
}

const bad = () => {
  return { type: "BAD" }
}

const zero = () => {
  return { type: "ZERO" }
}

export { good, ok, bad, zero }
